import 'package:flutter/material.dart';

/// Named [AppColors] in order to avoid conflicts with Colors in UI packages.
class AppColors {
  static const black = Color(0xff191919);
  static const regularGrey = Color(0xff454545);
  static const primary = Color(0xff1e1e21);
  static const secondary = Color(0xffededed);
  static const background = Color(0xfffdeaec);
  static const pink = Color(0xfff1b3b4);
}
